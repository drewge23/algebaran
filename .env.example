# Copy to .env.local and fill in from Supabase → Project Settings → API.
# Both values are safe in the client bundle; Row Level Security protects the data.
# Leave unset to run the app in local-only mode (no backend).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
